# Ticket: Improve Playlist Page UI

Goal:
- Modernize the playlist page layout by removing unnecessary headers, optimizing component alignment, and improving the empty state visual.

Scope:
- Remove the "Dance" header bar.
- Realign "search videos" and "add video" button to appear on the same line.
- Redesign the empty state: replace the current "+" icon with a more intuitive visual representation (e.g., a video icon or illustration).

Definition of done:
- The "Dance" header bar is no longer displayed.
- The search input and add button are horizontally aligned on the same row.
- The empty state UI displays a new visual element instead of the "+" icon.
- The UI remains responsive and functional across different screen sizes.

Steps to verify:
1. Navigate to the playlist page.
2. Confirm the "Dance" header is removed.
3. Verify the search bar and add button are on the same line.
4. If the library is empty, verify the new empty state visual is displayed instead of the "+" icon.
